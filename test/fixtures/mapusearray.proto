syntax = "proto3";

message Config {
  optional uint32         field               = 1;
  optional int32          type                = 2;
}

message Message {
  map<uint32, Config> config = 1;
  optional string     label = 2;
}
